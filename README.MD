# Notas
Este es mi primera app en node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es el numero a multiplicar                             [number]
  -l, --listar   mostrar o no la lista                [boolean] [default: false]
```